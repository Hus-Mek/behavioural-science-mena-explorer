version: "3.8"

services:
  explorer:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./results:/app/results
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    restart: unless-stopped
